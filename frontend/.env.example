# Backend address (server-side proxy /api/* -> BACKEND_URL/api/*)
BACKEND_URL=http://localhost:8000
# true = mock data instead of the backend (with a response switcher in the header)
NEXT_PUBLIC_USE_MOCKS=false
